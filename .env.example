# ORE Mining Configuration

# Path to your Solana wallet keypair file
# Default (auto-generated): ./tmp/keypair.json
# Or use global: /Users/yourusername/.config/solana/id.json
PRIVATE_KEY_PATH=./tmp/keypair.json

# Solana RPC URL
# Mainnet: https://api.mainnet-beta.solana.com
# Devnet: https://api.devnet.solana.com
# Custom RPC: https://your-rpc-provider.com
RPC_URL=https://api.mainnet-beta.solana.com

# Amount to bet per block in SOL (can be decimal)
# Example: 0.1 = 0.1 SOL
# Example: 100000000 = 100000000 lamports
BET_AMOUNT=0.1

# Number of blocks to randomly select and deploy to (1-25)
# The script will automatically:
#   1. Fetch available blocks from mainnet
#   2. Randomly select this many blocks
#   3. Deploy BET_AMOUNT to each selected block
# Example: 3 = randomly select 3 available blocks
# Example: 10 = randomly select 10 available blocks
BLOCKS_QUANTITY=3

# Optional: Delay between deployments in seconds (to avoid rate limiting)
# Default: no delay
DEPLOYMENT_DELAY=1

# Optional: Authority address (defaults to keypair pubkey if not set)
# AUTHORITY=YourSolanaAddressHere

